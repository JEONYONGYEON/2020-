#include <stdio.h>

int main(void)
{
	while(num != 0)	{
	int num;
    int numarr[6] = {0,};
    scanf("%s", &numarr);
    for(int i = 0; i != '\0'; i++)
	printf("%d", numarr[i]);

    }
	
	return 0;
}
