#incldue <stdio.h>

int main(void)
{
	for(int t = 1; t < T; t++)
    {
    	int y, m, d;
    	
    	scanf("%4d", y);
    	scanf("%2d", m);
    	scanf("%2d", d);
    
    if(m > 12 || m < 0) printf("%d", -1);
    else if
	 
    
	printf("#%d %d%d%d", t, y, m, d);	
	}
	
	return 0;
}
